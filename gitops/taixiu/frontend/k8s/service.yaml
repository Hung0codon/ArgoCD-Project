apiVersion: v1
kind: Service
metadata:
  name: taixiu-frontend
  namespace: demo
spec:
  type: NodePort
  selector:
    app: taixiu-frontend
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080
