apiVersion: v1
kind: Service
metadata:
  name: taixiu-backend
  namespace: demo
spec:
  type: NodePort
  selector:
    app: taixiu-backend
  ports:
  - port: 5000
    targetPort: 5000
    nodePort: 30050
